const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Governance", () => {
  // simple deployment checks
  it("deploys correctly", async () => {

  });

  // creation of proposals
  // test that only members can create proposals, ie, those with a MembershipNFT

  // voting on proposals
  // test that only members can vote on proposals
  // test the quorum requirement, that proposals can only pass if they meet the quorum
  // test the voting delay and period work as expected

  // test the setting of various parameters (delay, period, etc.)

  // grants
  // test the creation of grant-proposals
})
